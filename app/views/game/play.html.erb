<div class="container">
	<h1>Lets start</h1>

	<h2><%= @questions.first.title %></h2>

	<%= radio_button_tag(:answer, "a") %>
	<%= label_tag(:a, @questions.first.a) %>
	<%= radio_button_tag(:answer, "b") %>
	<%= label_tag(:b, @questions.first.b) %>



</div>

